export interface Drop {
	username: string;
	verified: string;
	profileImage: string;
	content: string;
	image?: string;
}
export interface Friend {
	name: string;
	username: string;
	avatar: string;
}
export interface News {
	title: string;
	subtitle: string;
	image: string;
}
